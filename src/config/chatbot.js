/**
 * Floating chatbot button position (web portals).
 * Adjust these values to move the FAB and chat panel.
 *
 * Examples:
 *   Bottom-right (default): { bottom: 24, right: 24 }
 *   Bottom-left:            { bottom: 24, left: 24 }
 *   Top-right:                { top: 88, right: 24 }  // below app bar
 */
export const CHATBOT_POSITION = {
  bottom: 200,
  right: 24,
  left: undefined,
  top: undefined,
};
